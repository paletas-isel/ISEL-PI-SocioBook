@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div id="content">
    <div id="user-login">
        @if (!User.Identity.IsAuthenticated)
        {    
            @Html.Partial("UserLogOnForm", Tuple.Create("LogOn", (string) null))
        }
        else
        {
            <h1>Welcome to <strong>SocioBook</strong>!</h1>
        }

    </div>				
</div>